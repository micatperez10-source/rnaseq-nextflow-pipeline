params {
    outdir = "./results"
}

profiles {
    test {
        params {
            input = "$launchDir/data/sample1_R1.fastq.gz"
        }
    }
    docker {
        docker.enabled = true
        docker.runOptions = '-u $(id -u):$(id -g) --platform linux/amd64 -v /Users:/Users'
    }
}